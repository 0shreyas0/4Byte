'use client';
import React from 'react';
import { SearchBarRaw } from './shared';

export default function Preview({ currentUser, displayUser }: any) {
  return (
        <div className="max-w-lg mx-auto py-12 w-full flex items-center justify-center">
          <SearchBarRaw placeholder="Search components..." />
        </div>
      );

    // ── Payment ───────────────────────────────────────────────────────────────
}