'use client';
import React from 'react';

export default function Preview({ currentUser, displayUser }: any) {
  return (
        <div className="border border-border rounded-xl overflow-hidden w-full relative z-20">
          <AppNavbar />
        </div>
      );
}